
public class Child extends Parent  //inheritance allowed
{ 
	//it gives method error
    public void show() //here we not override the parent class method because parent class method is final
    {
    	
    }
}
